apiVersion: v1
kind: Service

metadata:
  name: static-website-service
  namespace: static-website
spec:
  selector:
    app: static-website

  ports:
  - port: 80
    targetPort: 80
  type: NodePort

